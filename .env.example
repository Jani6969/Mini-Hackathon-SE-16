# ============================================================
#  Root .env.example — pointer only.
#  This project keeps environment variables per package.
# ============================================================
#
#  server/.env   MONGO_URI, PORT        -> see server/.env.example
#  client/.env   VITE_API_URL           -> see client/.env.example
#
#  Nothing is read from the repository root. Do not put secrets here.
#  Full reference and platform settings: docs/ENV_VARS.md
